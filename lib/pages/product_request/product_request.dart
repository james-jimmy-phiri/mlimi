import 'package:flutter/material.dart';

class ProductRequest extends StatelessWidget {
  const ProductRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
