import 'package:flutter/material.dart';

class ProductSaleOffs extends StatelessWidget {
  const ProductSaleOffs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
